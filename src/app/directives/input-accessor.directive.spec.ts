import { InputAccessorDirective } from './input-accessor.directive';

describe('InputAccessorDirective', () => {
  it('should create an instance', () => {
    const directive = new InputAccessorDirective();
    expect(directive).toBeTruthy();
  });
});
